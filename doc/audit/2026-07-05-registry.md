# Audit Registry — 2026-07-05

Full-codebase audit findings. Status: `todo` | `in-progress` | `fixed (<commit>)` | `wontfix (<reason>)`.
Verified = confirmed first-hand by reading code or empirical reproduction before registration.

## Critical

| ID  | Area         | Finding                                                                                                                                                                                                                                                                        | Verified            | Status |
| --- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------- | ------ |
| C1  | agent/stream | Runner `ch <-` sends never select on `ctx.Done()`; `ForwardWithHistory`/`Forward` exit on cancel without draining (violates architecture rule 3). Cancel during a delta burst fills the 64 buffer and leaks the Run goroutine + open HTTP stream forever.                      | yes                 | fixed  |
| C2  | agent/stream | Cancel drops in-flight `HistoryAppendMsg` events: assistant `tool_use` applied but tool-result message dropped → dangling `tool_use` → API 400 on every later request until `/clear`.                                                                                          | yes                 | fixed  |
| C3  | tui          | Queued deltas after Esc hit `handleStreamDelta` `!wasStreaming → startStream()` — spinner runs forever, input blocked until second Esc (`handlers.go:396-400`).                                                                                                                | yes                 | fixed  |
| C4  | tui          | `pendingQuestion` only cleared in `handleAnswerQuestion`; Esc/stream-error leave it set → next user message routed into dead channel and vanishes (`handlers.go:260,284,514`).                                                                                                 | yes                 | fixed  |
| C5  | cmd          | Data race: `*streamCancel` written from `tea.Cmd` goroutine (`main.go:188`), read from Update loop (`main.go:230-231`), unsynchronized.                                                                                                                                        | yes                 | fixed  |
| C6  | tools        | No approval gate: `bash`/`write_file`/`edit` execute whatever the model emits, while `web_fetch` pulls untrusted content → prompt-injection-to-RCE pipeline.                                                                                                                   | yes (by inspection) | fixed  |
| C7  | auth         | Token refresh is dead code (`RefreshAccessToken`/`NeedsRefresh` have zero production callers); `ResolveAPIKey` abandons OAuth when the _access token_ expires despite holding a valid API key + refresh token (`resolve.go:81-82`).                                            | yes                 | fixed  |
| C8  | auth         | `tokenSearchDirs` puts `workDir` first — a hostile repo with committed `.aql_tokens.json` silently hijacks the session; contradicts `credentialDir`'s own comment (`resolve.go:46-55`).                                                                                        | yes                 | fixed  |
| C9  | e2e          | Committed fixtures are recordings of failed sessions (record-api-call: 2× `context canceled`, empty bodies; edit-file-diff: 1× 200 + 6× 400 + 3× cancel); assertions are vacuous (`WaitFor("hello")` matches the echoed prompt; EditFileShowsDiff asserts nothing).            | yes (reproduced)    | fixed  |
| C10 | e2e          | Recorder writes full request headers incl. `X-Api-Key` into git-committed testdata; zero scrub code (current REDACTED was hand-edited). Next `E2E_RECORD=1` run commits a live key.                                                                                            | yes                 | fixed  |
| C11 | auth         | `ResolveAPIKey` searched the real user-config + home dirs unconditionally, so `TestResolveAPIKey_NoTokens*` read the developer's live tokens and failed — leaking a real API key into pre-commit output and blocking every commit on a logged-in machine (`resolve.go:60-95`). | yes (reproduced)    | fixed  |

## High

| ID  | Area  | Finding                                                                                                                                                                                                                                                                            | Verified         | Status |
| --- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ------ |
| H1  | tui   | `View()` rebuilds full transcript + constructs a new glamour renderer per text part per frame (50–120 ms ticks) — quadratic cost over a streaming response (`app.go:531-544`, `markdown.go:36`).                                                                                   | reviewer         | fixed  |
| H2  | agent | User message appended to history before the API call; a failed turn leaves a trailing user message → consecutive `user` roles rejected by API (`runner.go:92-94`); same shape when `maxToolIterations` exhausts.                                                                   | yes              | fixed  |
| H3  | agent | Mid-stream retry re-invokes `StreamMessage` and `onText` re-emits — transcript shows partial first attempt + full retry (`runner.go:159-165`).                                                                                                                                     | yes              | fixed  |
| H4  | tools | `bash`: no process group/`WaitDelay`; cancel kills only `sh`, orphaned grandchildren hold the pipe and `CombinedOutput` blocks forever; no timeout despite comment claiming ctx carries one (`shell.go:38-40`).                                                                    | yes              | todo   |
| H5  | tools | `web_fetch`: no scheme/host/private-IP restrictions — can read cloud metadata (169.254.169.254), localhost, intranet (`web.go:55-60`).                                                                                                                                             | reviewer         | todo   |
| H6  | llm   | Compaction `SendMessage` never sets `OAuthBilling` (only `runner.go:310` does) — `/compact` and auto-compact go out mis-configured on OAuth sessions; leaky design: every caller must re-assert auth (`compact.go`).                                                               | yes              | todo   |
| H7  | tools | `glob` fails its own documented example: `src/**/*.ts` returns "No files matched" (reproduced empirically; `filepath.Match` has no `**`, only leading `**/` special-cased) (`glob.go:90-107`, `defs.go:131`).                                                                      | yes (reproduced) | todo   |
| H8  | tui   | `/compact` sets `m.chat = nil` before async compaction runs; failure permanently destroys transcript (`app.go:418-422`).                                                                                                                                                           | reviewer         | todo   |
| H9  | agent | `Spawn` hand-builds child executors, dropping `WithAskUser` + shared HTTP client — the exact bug class the file's own header warns about (`spawner.go:101-107`).                                                                                                                   | reviewer         | fixed  |
| H10 | llm   | `consumeStream` ignores `message_start` (documented carrier of `input_tokens`) — auto-compact may never fire; also drops thinking blocks while OAuth config enables adaptive thinking (400 risk); `SendMessage` silently drops `tool_use` blocks (`anthropic.go:307-347,149-169`). | yes (grep)       | fixed  |
| H11 | e2e   | Recorder stores raw gzip bodies in JSON strings — invalid UTF-8 → U+FFFD, irreversibly corrupted at rest; replayer serves strictly sequentially while recording order is body-Close order (`recorder.go:170-183`, `replayer.go:39-48`).                                            | reviewer         | todo   |

## Medium

| ID  | Area   | Finding                                                                                                                                                                                                                                                           | Verified | Status |
| --- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------ |
| M1  | auth   | `BuildAuthorizeURL` joins scopes with a literal space — malformed URL per RFC 3986; test enshrines the hack (`oauth.go:124-135`).                                                                                                                                 | reviewer | todo   |
| M2  | auth   | `ExchangeCode`/`CreateAPIKey`/`RefreshAccessToken` take no context, use `http.DefaultClient` with no timeout — stalled token endpoint hangs login forever (`oauth.go:152,202,239`).                                                                               | reviewer | todo   |
| M3  | auth   | Any `/callback` hit with wrong/missing state pushes to `errCh` and aborts the login; real callback arriving later is lost (`login.go:81-96,186-193`).                                                                                                             | reviewer | todo   |
| M4  | models | Probes run unbounded-parallel with `max_tokens:1024`, re-run hourly, 429 treated as "unusable" → can rate-limit itself into an empty model list while burning quota (`probe.go:53,165-180,217`).                                                                  | reviewer | todo   |
| M5  | tools  | `read_file` and `bash` output uncapped while every other tool truncates — one big read bloats every subsequent request (`file.go:41-45`, `shell.go:40-45`).                                                                                                       | reviewer | todo   |
| M6  | tui    | `mergeToolResult` only searches the current assistant block; an interposed user/status entry leaves the tool rendered "running…" forever (`transcript.go:260-272`).                                                                                               | reviewer | todo   |
| M7  | tui    | `visibleHeight()` never subtracts palette/model picker/task panel — open overlays overflow the frame and clip status bar/prompt (`app.go:295-304`).                                                                                                               | reviewer | todo   |
| M8  | tui    | `scrollUp` accumulates unbounded offset; render-time clamp computed on a copy, never persisted — phantom scroll hysteresis; same in diff view (`app.go:281-284`, `diff.go:92,232-239`).                                                                           | reviewer | todo   |
| M9  | tui    | Transcript search is a stub: `scrollToTranscriptMatch` scrolls to bottom on every match, `n`/`N` decorative (`app.go:663-671`).                                                                                                                                   | reviewer | todo   |
| M10 | tui    | Search-mode key handler appends `msg.String()` for special keys ("up", "ctrl+c" typed into query; can't quit from search) (`app.go:651-657`).                                                                                                                     | reviewer | todo   |
| M11 | tui    | `addStatusChat` also clears the input buffer and is called from async completions — erases whatever the user is typing (`app.go:369-377`).                                                                                                                        | reviewer | todo   |
| M12 | tui    | `/model sonnet` (palette's own example) and any mistyped slash command fall through to the LLM as a chat prompt (`commands.go:43`, `app.go:381-439`).                                                                                                             | reviewer | todo   |
| M13 | tui    | Streaming indicator has no trailing newline, task panel no leading one — panel glues onto the "Composing…" line (`app.go:499-515`, `tasks.go:168`).                                                                                                               | reviewer | todo   |
| M14 | domain | `domain` hosts Claude Code `BillingHeader`/`ClaudeCodeBetas` + display-oriented `ToolCall`, violating its own MUST-NOT list; 4 near-identical `{ID,Name,Input}` tool shapes + duplicate `ToolDef` with manual copy loop (`types.go`, `runner.go:284-291`).        | reviewer | todo   |
| M15 | models | `models` is a second parallel SDK adapter duplicating billing-header injection from `llm`; core `agent` imports `models` (I/O adapter) for a pure 15-line name map (`probe.go:209-218` vs `anthropic.go:193-202`, `runner.go:100`).                               | reviewer | todo   |
| M16 | cmd    | Two independent `sh -c` executors (TUI `!` in `main.go:202-211` vs agent bash tool) with divergent timeout/limit policies.                                                                                                                                        | yes      | todo   |
| M17 | agent  | `enrichAPIError` lumps 400 with 403 — invalid-request 400s (e.g. corrupted history) diagnosed as auth/model-access problems (`errors.go:78-81`).                                                                                                                  | reviewer | todo   |
| M18 | e2e    | Session-wide singleton recorder: every recording test's cleanup saves ALL session exchanges into its own fixtureDir — cross-contamination when recording >1 test; `sharedProxy` never closed; upstream URL hardcoded (`session.go:48-55`, `terminal.go:203-210`). | reviewer | todo   |

## Low

| ID  | Area   | Finding                                                                                                                                                                                                                                                                                                                                                         | Verified                         | Status |
| --- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- | ------ |
| L1  | tui    | Dead code: `agent_panel.go` (superseded design), `RenderPrompt`/`RenderPromptAreaStreaming`, `SpinnerTick`/`RenderSpinner`, `OSC52Copy`, `RenderLogo`, `Contains`, `AgentOutputMsg`/`AgentStatusMsg` never sent, `ClipboardMsg` never consumed, empty `header.go`, unreachable `/exit` cases in `executeCommand`, unreachable diff loading branch (~250 lines). | reviewer                         | todo   |
| L2  | tui    | Byte-slicing multibyte strings: `transcript.go:137,171,178` truncation, `handlers.go:68` + `app.go:649` backspace — invalid UTF-8 output/corrupted editing; `truncate.go` exists for exactly this.                                                                                                                                                              | reviewer                         | todo   |
| L3  | tui    | Status bar gap math uses byte `len()` on strings with "·" — right edge off by 2 per hint; picker custom-ID input rejects non-ASCII and paste (`statusbar.go:30-36`, `handlers.go:79`).                                                                                                                                                                          | reviewer                         | todo   |
| L4  | tui    | `FormatToolSummary` bash case byte-identical to default (dead branch); collapsed headers produce "Updateing 3 files…"/"Bash 3 files" (`transcript.go:168-180,478-480`).                                                                                                                                                                                         | reviewer                         | todo   |
| L5  | tui    | ~15 exported "(for testing)" accessors incl. mutator `HandleToolCallExported` — production API doubles as test backdoor (`app.go:607-722`, `tasks.go:102-119`).                                                                                                                                                                                                 | reviewer                         | todo   |
| L6  | tui    | `handleTaskToolResult` returns a bool nobody reads and swallows JSON errors silently, contra slog rules (`tasks.go:54-88`).                                                                                                                                                                                                                                     | reviewer                         | todo   |
| L7  | tools  | grep pattern passed positionally without `--` — model patterns starting with `-` parsed as flags (`shell.go:61-65`).                                                                                                                                                                                                                                            | reviewer                         | todo   |
| L8  | agent  | `GitStatus` + date baked into system prompt at construction, only refreshed on CLAUDE.md mtime change — stale after first commit; `RefreshClaudeMD` holds `a.mu` across file I/O + two git execs (`agent.go:147,275`, `env.go:58`).                                                                                                                             | reviewer                         | todo   |
| L9  | agent  | Spawner: all parallel children named identically (`coder-sub-1`); child result concatenates every delta across iterations (narration included); child `TokenUsage` dropped → `/cost` undercounts (`spawner.go:91,117-124`).                                                                                                                                     | reviewer                         | todo   |
| L10 | llm    | `WithBearerToken` has no production caller — dead auth path (`anthropic.go:96-98`).                                                                                                                                                                                                                                                                             | yes (reviewer + resolve.go read) | todo   |
| L11 | auth   | `LoginResult` exported but used only in tests (`login.go:36-40`); `io.ReadAll` errors discarded → "unexpected end of JSON input" instead of network error (`oauth.go:158,208`).                                                                                                                                                                                 | reviewer                         | todo   |
| L12 | models | `BillingHeader`/`ClaudeCodeBetas` "re-exported for backwards compatibility" in an internal package with no external consumers (`probe.go:55-59`); `string(ResolveModel(""))` no-op cast (`startup.go:46`).                                                                                                                                                      | reviewer                         | todo   |
| L13 | diff   | `extractFilePath` splits on single space — paths with spaces parse wrong, hunks silently dropped (`parse.go:194-204`).                                                                                                                                                                                                                                          | reviewer                         | todo   |
| L14 | cmd    | `configureTUI` is 78 lines threading `**tea.Program` + `*context.CancelFunc` double pointers; produced C5 (`main.go:177-255`).                                                                                                                                                                                                                                  | yes                              | todo   |
| L15 | agent  | `CollectClaudeMD` variadic with exactly one single-dir call site — speculative generality (`context.go:43`).                                                                                                                                                                                                                                                    | reviewer                         | todo   |

## Deliberately out of scope (recorded, not fixed in this pass)

| ID  | Area | Finding                                                                                                                           | Reason                                                                                                                                                |
| --- | ---- | --------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| X1  | tui  | God-struct `Model` (20+ fields, 3 modal dispatch points) + hand-rolled `textinput`/`viewport`/`spinner` instead of stock bubbles. | Multi-day rewrite; risk of regressing the whole TUI. Mitigations M7 (height) and H1 (render cost) fixed individually. Needs its own planned refactor. |
